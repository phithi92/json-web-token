{
    "name": "phithi92/json-web-token",
    "description": "Handles the generation of JSON Web Tokens (JWT), including both JWS (JSON Web Signature) and JWE (JSON Web Encryption). Provides methods for creating and validating tokens.",
    "type": "library",
    "license": "MIT",
    "version": "0.2.8",
    "authors": [
        {
            "name": "Phillip Thiele",
            "email": "development@phillip-thiele.de"
        }
    ],
    "minimum-stability": "dev",
    "require": {
        "php": ">=8.2",
        "phpseclib/phpseclib": "~3.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0",
        "phpbench/phpbench": "^1.0",
        "nikic/php-parser": "^5.3",
        "phpstan/phpstan": "2.0.x-dev",
        "nunomaduro/phpinsights": "dev-master",
        "shipmonk/dead-code-detector": "^0.9.2"
    },
    "autoload": {
        "psr-4": {
            "Phithi92\\JsonWebToken\\": "src/",
            "Tests\\": "tests/"
         }
    },
    "scripts": {
        "test": "phpunit --configuration phpunit.xml",

        "analyze": "@php vendor/bin/phpstan analyse --configuration=phpstan.neon --memory-limit=1G",
        "style": "phpcs --standard=PSR12 src/",

        "benchmark": "@php vendor/bin/phpbench run tests/phpbench/* --report=benchmark",

        "generate-keys": "@php scripts/generate-keys.php"
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    }
}
